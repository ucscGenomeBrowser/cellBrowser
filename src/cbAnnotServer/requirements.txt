Flask>=3.0
Flask-Login>=0.6
Flask-SQLAlchemy>=3.1
Flask-Mail>=0.9
SQLAlchemy>=2.0
gunicorn>=21.2
Authlib>=1.3          # OpenID Connect sign-in (providers configured in providers.conf)
requests>=2.31        # HTTP transport Authlib uses to reach the OIDC endpoints
